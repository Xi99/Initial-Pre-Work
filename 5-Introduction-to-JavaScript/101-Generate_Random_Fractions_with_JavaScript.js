// The Math.random() function returns a random decimal number between 0 (inclusive) and not quite up to 1 (exclusive)
function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
