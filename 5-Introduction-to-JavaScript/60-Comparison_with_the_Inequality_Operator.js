// The inequality operator "!=" is the opposite of the equality operator "==". It is to say if something "is not" equal, then it is true and vice versa.
// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(99);
