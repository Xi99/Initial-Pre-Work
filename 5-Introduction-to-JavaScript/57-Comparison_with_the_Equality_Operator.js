// The equality operator "==" compares two values and will return either true if they are equivalent and false if they aren't. Data types are converted by JS so they can be compared.
// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(12);
