// You can refactor your code for if / else statements with boolean valuse with a comparison operator.
function isLess(a, b) {
  // Fix this code
  return a < b;

}

// Change these values to test
isLess(10, 15);
