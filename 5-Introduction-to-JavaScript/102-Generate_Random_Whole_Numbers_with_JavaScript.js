// We can use the Math.random() and multiply it to get numbers above 0. We can then put that result in the Math.floor() to get the number to round down to the nearest whole number.
var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random() * 10);
}
